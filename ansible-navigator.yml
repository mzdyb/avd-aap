---
ansible-navigator:
  ansible:
    config:
      path: ansible.cfg
  execution-environment:
    image: aap.rh.lab/ee-arista-avd:1.0
    pull:
      policy: missing
  playbook-artifact:
    enable: false
  mode: stdout

